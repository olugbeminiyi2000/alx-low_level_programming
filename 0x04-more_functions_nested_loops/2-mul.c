#include "main.h"
/*Write a function that multiplies two integers.*/
/**
 * mul - multiplies two numbers
 * @a: first parameter of the function
 * @b: second parameter of the function
 *
 * Return: returns the product in int dtype
 */
int mul(int a, int b)
{
	return (a * b);
}
